# Deko: Formally Verified SM-Based Intra-VM Compartmentalization for CVMs

## Build

We have provided you with a Dockerfile to build the image and play with the code. To build the image, run:

```bash
docker build -t deko:latest .
```

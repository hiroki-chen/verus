FROM python:3.12-slim

ARG SCRIPT_PATH

WORKDIR /app

COPY scripts /app/scripts

RUN test -n "${SCRIPT_PATH}" \
    && test -f "/app/scripts/${SCRIPT_PATH}" \
    && chmod +x "/app/scripts/${SCRIPT_PATH}"

CMD ["python3", "/app/scripts/orders_ingest.py"]

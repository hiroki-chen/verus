FROM python:3.12-slim

ARG SCRIPT_PATH

WORKDIR /app

COPY tests/guest/scripts /app/scripts

RUN test -n "${SCRIPT_PATH}" \
    && test -f "/app/${SCRIPT_PATH}" \
    && chmod +x "/app/${SCRIPT_PATH}"

CMD ["python3", "/app/scripts/orders_ingest.py"]

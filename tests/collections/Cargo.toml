[package]
name = "collections"
version = "0.1.0"
edition = "2024"

[dependencies]
deko-core = { path = "../../deko-core", features = ["alloc"] }
vstd = { git = "https://github.com/hiroki-chen/verus.git", default-features = false, features = [
  "alloc",
] }
proptest = "1.9.0"
indicatif = "0.17.8"

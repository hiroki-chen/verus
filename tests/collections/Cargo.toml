[package]
name = "collections"
version = "0.1.0"
edition = "2024"

[dependencies]
deko-core = { workspace = true, features = ["alloc"] }
vstd = { workspace = true, features = ["alloc"] }
proptest = { workspace = true }

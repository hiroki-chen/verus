[package]
name = "deko-stage1"
version = "0.1.0"
edition = "2021"


[package.metadata.verus]
verify = false

[dependencies]
deko-meta = { workspace = true }
uefi = { version = "0.35.0", features = ["logger", "panic_handler"] }
uefi-raw = { version = "0.11.0" }
xmas-elf = "0.10.0"

[profile.release]
lto = true
panic = "abort"
strip = true

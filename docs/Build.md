# Build

1. Build the initial bootloader that starts at the reset vector. (deko-monitor)
2. Build the stage2 bootloader that sets up the initial environment for "true" deko monitor (deko-core/src/stage2.rs)
3. Build the deko monitor (deko-core/src/deko.rs).

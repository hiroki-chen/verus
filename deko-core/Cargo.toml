[package]
name = "deko-core"
edition = "2021"

[package.metadata.verus]
verify = true

[dependencies]
vstd = { workspace = true }
verus_builtin = { workspace = true }
verus_builtin_macros = { workspace = true }

deko-meta = { workspace = true }
deko-macros = { workspace = true }
deko-std = { workspace = true, features = ["alloc"] }
deko-logging = { workspace = true, optional = true }
log = { version = "0.4.27", optional = true }

[features]
default = ["snp", "logging"]
tdx = []
snp = []
# Enable logging?
logging = ["deko-logging", "log"]

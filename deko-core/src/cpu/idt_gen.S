
    // Auto-generated by build.rs - do not edit.
    .code64
    .pushsection .entry.text, "ax"
    .macro push_regs
        pushq	%rax
        pushq	%rbx
        pushq	%rcx
        pushq	%rdx
        pushq	%rsi
        pushq	%rdi
        pushq	%rbp
        pushq	%r8
        pushq	%r9
        pushq	%r10
        pushq	%r11
        pushq	%r12
        pushq	%r13
        pushq	%r14
        pushq	%r15
        xorl	%eax, %eax
	    rdsspq	%rax
	    pushq	%rax
    .endm

    
        .globl de_handler
    de_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_de
        sti
    .Lskip_de:
        movl	$0, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl db_handler
    db_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_db
        sti
    .Lskip_db:
        movl	$1, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl nmi_handler
    nmi_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_nmi
        sti
    .Lskip_nmi:
        movl	$2, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl bp_handler
    bp_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_bp
        sti
    .Lskip_bp:
        movl	$3, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl of_handler
    of_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_of
        sti
    .Lskip_of:
        movl	$4, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl br_handler
    br_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_br
        sti
    .Lskip_br:
        movl	$5, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl ud_handler
    ud_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_ud
        sti
    .Lskip_ud:
        movl	$6, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl nm_handler
    nm_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_nm
        sti
    .Lskip_nm:
        movl	$7, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl df_handler
    df_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_df
        sti
    .Lskip_df:
        movl	$8, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_double_fault
        jmp	default_return
    
        .globl ts_handler
    ts_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_ts
        sti
    .Lskip_ts:
        movl	$10, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl np_handler
    np_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_np
        sti
    .Lskip_np:
        movl	$11, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl ss_handler
    ss_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_ss
        sti
    .Lskip_ss:
        movl	$12, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl gp_handler
    gp_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_gp
        sti
    .Lskip_gp:
        movl	$13, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_general_protection
        jmp	default_return
    
        .globl pf_early_handler
    pf_early_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_pf_early
        sti
    .Lskip_pf_early:
        movl	$14, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_page_fault_early
        jmp	default_return
    
        .globl pf_handler
    pf_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_pf
        sti
    .Lskip_pf:
        movl	$14, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_page_fault
        jmp	default_return
    
        .globl mf_handler
    mf_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_mf
        sti
    .Lskip_mf:
        movl	$16, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl ac_handler
    ac_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_ac
        sti
    .Lskip_ac:
        movl	$17, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl mce_handler
    mce_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_mce
        sti
    .Lskip_mce:
        movl	$18, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl xm_handler
    xm_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_xm
        sti
    .Lskip_xm:
        movl	$19, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl ve_handler
    ve_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_ve
        sti
    .Lskip_ve:
        movl	$20, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_ve
        jmp	default_return
    
        .globl cp_handler
    cp_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_cp
        sti
    .Lskip_cp:
        movl	$21, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl vc_handler
    vc_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_vc
        sti
    .Lskip_vc:
        movl	$29, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_vmm_handler
        jmp	default_return
    
        .globl sx_handler
    sx_handler:
        clac

    
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_sx
        sti
    .Lskip_sx:
        movl	$30, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_panic
        jmp	default_return
    
        .globl int80_handler
    int80_handler:
        clac

    	pushq	$0
        push_regs
        testl	$0x200, 0x98(%rsp)
        jz	.Lskip_int80
        sti
    .Lskip_int80:
        movl	$128, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_syscall_handler
        jmp	default_return
    
        .globl irq_ipi_handler
    irq_ipi_handler:
        pushq	$0
        push_regs
        movl	$224, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_irq_ipi
        jmp	default_return
    
        .globl irq_int_inj_handler
    irq_int_inj_handler:
        pushq	$0
        push_regs
        movl	$80, %esi
        movq	%rsp, %rdi
        xorl	%edx, %edx
        call	ex_handler_irq_int_inj
        jmp	default_return
    
    .popsection
    
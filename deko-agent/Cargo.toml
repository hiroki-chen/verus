[package]
name = "deko-agent"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
deko-policy-format = { path = "../deko-policy-format", features = ["alloc"] }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"

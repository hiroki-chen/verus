[x] Check if it is really necessary for us to support IGVM. If so, how.
[x] Check `td-shim` crate.
  - [x] What confuses me here is that this crate can be built as "Linux Boot Protocol" and "to launch an executable payload" or "ELF format payload". What are the differences?

[X] Check if we really need to support TDVF section 5 as QEMU hasn't added support for direct Linux boot yet, but CloudHypervisor has.
[ ] Implement debug logging (due to the lack of gdb support we have to resort to debug logging)!
  [x] Enable serial ports => FOR TDX
  [ ] Lock (in SMP systems)
  This can be an unverified logger as we don't need it in production environment. So it is safe to keep one.

[ ] Code refactoring: We may split specs, proofs, and execs into different Rust file for both better readability and maintainablity.
[ ] How to lift a physical pointer into a virtual pointer with provenance and physical memory ranges properties?

Use this_page_perm itself to reason about the finer-grained properties is no good choice. This does not automatically sync with the value modified.
We need to change to `this_page` which is a verification-only struct. 


`#[verus_spec]` is somehow unstable.


Buddy allocator should take as input the virtual addresses, physical addresses and size to do the proper conversion between virtual addresses and physical addresses!